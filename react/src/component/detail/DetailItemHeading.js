import React from "react"
import styled from "styled-components"

import theme from "../../theme"

const DetailItemHeading = styled.h3`
margin: 0 0 .5vw 0;
font-size: ${theme.size.medium};
font-style: italic;
`;

export default DetailItemHeading