import React from "react"
import styled from "styled-components"

import theme from "../theme"
import PageTitle from "../component/PageTitle";

const Email = (props) => {
  return <PageTitle>email</PageTitle>
};

export default Email